# .env.example
# Copiez ce fichier en .env et remplissez les valeurs
# 
# Note: Cet outil fonctionne actuellement en mode client-side (Option A).
# Ce fichier est prévu pour une éventuelle migration vers Option B (avec backend).
# ============================================
# Configuration serveur
# ============================================
# Port d'écoute du serveur
# Type: number
# Valeur par défaut: 3000
# [OPTIONNEL]
PORT=3000

# Host d'écoute (0.0.0.0 pour toutes les interfaces)
# Type: string
# Valeur par défaut: 0.0.0.0
# [OPTIONNEL]
HOST=0.0.0.0

# Environnement (development | production)
# Type: string
# Valeur par défaut: production
# [OPTIONNEL]
NODE_ENV=production

# Niveau de logging (debug | info | warn | error)
# Type: string
# Valeur par défaut: info (dev) ou error (production)
# - debug: Tous les logs (développement uniquement)
# - info: Logs INFO, WARN, ERROR
# - warn: Logs WARN, ERROR
# - error: Seulement les erreurs (production par défaut)
# [OPTIONNEL]
LOG_LEVEL=info
