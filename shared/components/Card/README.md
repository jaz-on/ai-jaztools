# Composant `Card`

Composant carte réutilisable, compatible design system feed-minitools.

## API
```js
import Card from './Card.js';
const maCard = Card({ children: 'Contenu de la carte' });
document.body.appendChild(maCard);
```

## Props
- `children` : contenu de la carte (texte, nœud DOM ou tableau)
