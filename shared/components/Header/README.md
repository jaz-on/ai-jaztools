# Composant `Header`

Composant header réutilisable, compatible design system feed-minitools.

## API
```js
import Header from './Header.js';
const monHeader = Header({ title: 'Titre', subtitle: 'Sous-titre' });
document.body.appendChild(monHeader);
```

## Props
- `title` : titre principal
- `subtitle` : sous-titre
